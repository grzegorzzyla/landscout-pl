# --- sekrety / lokalna konfiguracja (NIGDY do repo) ---
site/.env
**/.env
# Kopie konfiguracji do wgrania w Portainerze — zawierają hasło, sekret sesji i tokeny.
*.env
!.env.example
.claude/settings.local.json

# --- dane prywatne użytkownika (baza ofert, zdjęcia, konfiguracja) ---
knowledge/
properties/listings/*
!properties/listings/.gitkeep
properties/photos/*
!properties/photos/.gitkeep
properties/facebook_groups.json
kryteria-poszukiwania.pdf
dd_section_*.md

# --- zależności i build ---
node_modules/
site/node_modules/
site/dist/
site/.astro/

# --- cache / tymczasowe ---
**/__pycache__/
*.pyc
scripts/.cache/

# --- sesja Facebook (zalogowany profil Playwright — NIGDY do repo) ---
# (wzorzec z gwiazdką łapie też odłożone kopie profilu — mają ciasteczka sesji)
scripts/.fb_profile*/
scripts/fb_cookies*.json

# --- junction do properties/photos (duplikat danych) ---
site/public/photos

# --- system ---
Thumbs.db
.DS_Store
promo-posty.md
